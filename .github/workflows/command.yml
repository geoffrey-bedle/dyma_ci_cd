name: commands
on: 
  push:
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo ::notice::Un message d'information
          echo ::warning::Un message d'avertissement        
          echo ::error::Un message d'erreur
